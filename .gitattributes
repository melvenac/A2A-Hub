# The SIA record and its rendered views are written LF by open-brain; keep them byte-exact in every checkout.
.agents/** text eol=lf
# Installed on tcm (Linux) as-is: a CRLF bash script does not run.
scripts/tcm/* text eol=lf
