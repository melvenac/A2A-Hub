# E2 — evidence for D2 (builder: cursor-peer)

Planner spec: `.agents/HOH/D2.md`. Follows accepted E1 (upsert + leftover pre-D1 duplicates).

## Files changed

| File | Change |
|---|---|
| `convex/agents.ts` | `register` collects `by_name`, keeps **max `lastSeen`** (tie-break `_id`), patches that row, deletes extras (cap 4000 writes/mutation) |
| `src/index.ts` | `GET /a2a/agents/live` includes `rowCount` (online table rows for that name **before** the HTTP name-map) |
| `.agents/SYSTEM/ENTITIES.md` | Collapse + `rowCount` documented |
| `.agents/HOH/E2.md` | This file |

Not touched: JSON-RPC mount, `hub-talk.mjs`, live `kind=ide-session` filter, daemon poll, D1 upsert (insert-if-missing still; patch if present). `AUTH_MODE` unchanged.

## Commands

```
npx convex deploy --url http://127.0.0.1:3210
npm test
npm run build
# restarted hub on :4000 from dist (rowCount is HTTP)
POST /a2a/register  { name: "cursor-peer", ... kind: ide-session }
```

`npm test`: **41/41 passed** (vitest 4.1.0, 1.21s). Exit 0.

GitNexus `impact` unavailable. Manual: `register` LOW–MEDIUM (deletes extras for the registered name only). `/a2a/agents/live` LOW (additive field; hub-talk only reads `agents[].name`).

## Acceptance (1–2) — collapse + observable rowCount

**Before** (Convex `listOnline`): `cursor-peer` **857**, `cursor-grok` **856**.

**After** one `POST /a2a/register` for `cursor-peer`: `cursor-peer` **1** (`_id` `j57ew1aa191vaqdpbadgtk3b0h8ec911`).

`GET /a2a/agents/live?kind=ide-session` after hub restart:

```json
{"name":"cursor-peer","kind":"ide-session","rowCount":1}
{"name":"cursor-grok","kind":"ide-session","rowCount":1}
```

`rowCount === 1` for `cursor-peer` is the collapse, not the HTTP map: Convex `listOnline` also has exactly one row.

`cursor-grok` also dropped to 1 in Convex because their next hub-talk `register()` ran after this mutation deployed (same self-heal). alice=13, bob=13, scout=2, gitnexus=4 remain until those names register.

## Preserve check

`/health` 200, Convex ok. `POST /a2a/jsonrpc` still JSON-RPC 2.0 (method-not-supported error envelope, not HTTP 404). Live still filters `kind=ide-session`.
