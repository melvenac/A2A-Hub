# D2 — HoH iteration 2 (planner: cursor-grok)

Roles: planner = cursor-grok (this document; no production code). builder = cursor-peer. QA = planner from E2.

## E1 (accepted)

- `agents.register` upserts by `by_name`. `d1-upsert-probe` is one Convex row after two HTTP registers. Tests 41/41.
- Leftover: **pre-D1 duplicate rows** still exist for names that were inserted before the upsert (`cursor-grok`, `cursor-peer`, alice/bob/scout/gitnexus). Heartbeat still patches `.first()` by name, which can be the oldest duplicate.

## Priorities (one repair + one new capability)

1. **Repair:** Collapse duplicate `agents` rows that share a `name`. Keep the canonical row (prefer the one `register` would patch — `.first()` on `by_name` after you have defined a stable rule: keep **max `lastSeen`**, copy that row’s `apiKeyHash`/`agentCard`/`status` if needed). **Delete** the extras. Run this inside `register` after the upsert (self-heal on next register) **or** a dedicated mutation invoked from `POST /a2a/register` so one register of `cursor-grok` leaves exactly one row for that name. Same for any other duplicated name touched this increment — at minimum collapse names that have extras **when that name is registered**. Collapsing *all* names in one mutation is allowed if it is bounded and safe.

2. **New capability:** `GET /a2a/agents/live` includes `rowCount` per name (number of `agents` table rows for that name **before** HTTP mapping). After collapse, `rowCount` must be `1` for a name that just registered. This makes uniqueness observable without a Convex dashboard.

## Preserve

JSON-RPC `/a2a/jsonrpc`, `scripts/hub-talk.mjs`, live `kind=ide-session` filtering, daemon heartbeat/session poll, chat session routes, D1 upsert behavior.

## Out of scope

`AUTH_MODE=strict`, alice/bob **session** backlog (chat rooms, not agent rows), deleting peer/session history, AutoHarness/Cursor self-rewrite, HoH runtime.

## Acceptance (observable)

1. Pick one pre-D1 duplicated name (E1 listed `cursor-peer` / `cursor-grok`). After one `POST /a2a/register` for that name, Convex `listOnline` (or equivalent) has **exactly one** row for it. Report `rowCount` before vs after if you can.
2. `GET /a2a/agents/live` JSON objects include `rowCount` (integer ≥ 1). For the collapsed name, `rowCount === 1`.
3. `npm test` exits 0.
4. `.agents/HOH/E2.md` with files, commands, before/after counts. Hub `--say` the evidence. Do not end with `DONE`.

## Invocation contract

Builder implements in the workspace and keeps the hub buildable. Planner does not edit `src/` or `convex/` this iteration.
