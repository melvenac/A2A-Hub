# D1 — HoH iteration 1 (planner: cursor-grok)

Paper: arXiv:2609.01481v1 (Harness-of-Harness). bib.bib36 = AutoHarness (Lou et al., 2603.03329) — they optimize the *harness*; we do **not** rewrite Cursor. We use the existing hub as the portal and run HoH *roles* on this repo.

Roles: planner = cursor-grok (this document; no production code). builder = cursor-peer. QA = planner on the next turn from builder evidence.

## S (public spec, this increment)

A2A-Hub is a local coordination server. IDE sessions already talk through `scripts/hub-talk.mjs` and `GET /a2a/agents/live?kind=ide-session`.

## E0 (evidence before D1)

- `agents.register` always `insert`s (`convex/agents.ts`). Re-register duplicates rows. Known debt; live directory must dedupe in HTTP because of this.
- Heartbeat patches `.first()` by name, so it may refresh a *stale* row while a newer insert sits beside it.
- Verified working (do not break): JSON-RPC `/a2a/jsonrpc`, hub-talk auto-join, chat sessions, wrapper daemons, `GET /a2a/agents/live`.

## Priorities (one repair + one new capability)

1. **Repair:** upsert `agents.register` by `by_name`. If a row exists, patch `apiKeyHash`, `agentCard`, `lastSeen`, `status: "online"` and return that id. Else insert.
2. **New capability:** builder writes `.agents/HOH/E1.md` with files touched, commands run, and the live-directory check result so the next plan has evidence.

## Preserve

JSON-RPC mount, `hub-talk.mjs` join/wait/say, `/a2a/agents/live` behavior for `kind=ide-session`, daemon register/heartbeat/session poll, chat session routes.

## Out of scope

Auth strict mode, alice/bob session backlog, full HoH runtime, AutoHarness-style self-modifying Cursor harness, refactoring hub-talk.

## Acceptance (observable)

1. Register the same agent name twice (HTTP `POST /a2a/register` or two `agents.register` calls). Unique names in the agents table for that name = 1 (or live endpoint lists that name once without HTTP-side name-map being the only reason).
2. `npm test` exits 0.
3. `.agents/HOH/E1.md` exists with: files changed, test output summary, how (1) was checked.
4. Builder reports on the hub session (hub-talk `--say`). Do not end a message with `DONE`.

## Invocation contract

Builder implements in the existing workspace, keeps the project buildable, posts evidence on the hub. Planner does not edit `src/` or `convex/` this iteration.
