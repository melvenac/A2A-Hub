# E1 — evidence for D1 (builder: cursor-peer)

Planner spec: `.agents/HOH/D1.md`. Paper: arXiv:2609.01481v1 (HoH). This increment is a register upsert, not a Cursor rewrite.

## Files changed

| File | Change |
|---|---|
| `convex/agents.ts` | `agents.register` looks up `by_name`; existing row is patched (`apiKeyHash`, `agentCard`, `lastSeen`, `status: "online"`) and that id is returned; else insert |
| `.agents/SYSTEM/ENTITIES.md` | Documented upsert-by-name (behavior; schema unchanged) |
| `src/index.ts` | Comment on `/a2a/agents/live` (register upserts; HTTP name-map still collapses pre-D1 duplicate rows) |
| `scripts/a2a-compliance-probe.mjs` | Comment: register no longer inserts unconditionally |
| `.agents/HOH/E1.md` | This file |

Not touched: JSON-RPC mount, `hub-talk.mjs`, `/a2a/agents/live` response shape, daemon poll paths, chat session routes, `AUTH_MODE`.

## Commands

```
npx convex deploy --url http://127.0.0.1:3210   # functions pushed; upsert is live
npm test                                        # vitest run
```

`npm test`: **41/41 passed** in 6 files (vitest 4.1.0, duration 1.80s). Exit 0.

GitNexus `impact` was unavailable this session. Manual blast radius on `register`: LOW. Sole production caller is `POST /a2a/register` in `src/index.ts` (ignores returned id). `heartbeat` still patches `.first()` by name; that is now the same row register writes.

## Acceptance (1) — same name registered twice is one row

Probe name `d1-upsert-probe` (not a prior peer):

1. `POST /a2a/register` twice with the same `{ name, apiKey, agentCard }`. Both returned HTTP 200 `{ ok: true }`.
2. Direct Convex query `agents.listOnline` (bypasses the HTTP name-map):

```
probeCount: 1
probeIds: ["j576h7tkq1qk06t2xscqmfxs218efva8"]
```

3. `GET /a2a/agents/live?kind=ide-session` listed `d1-upsert-probe` once.

The HTTP map is therefore not the only reason the name appears once: the agents table has a single `_id` for that name.

Pre-D1 duplicate rows remain for names that were re-inserted before this deploy (`cursor-peer`, `cursor-grok`, alice/bob/scout/gitnexus). Upsert does not collapse those; it only prevents new inserts. Cleaning them is out of scope for D1.

## Preserve check

Hub still serving `/health` 200 with Convex ok after the function push. Live directory still filters `kind=ide-session`. No hub-talk or JSON-RPC edits.
