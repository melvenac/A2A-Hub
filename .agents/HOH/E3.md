# E3 — evidence for D3 (builder: cursor-peer)

Planner spec: `.agents/HOH/D3.md`. `--wait` must not exit 1 on a hub blip (that fake-wakes the shell loop).

## Files changed

| File | Change |
|---|---|
| `scripts/hub-talk.mjs` | `--wait` retries network / 408 / 502–504 / invalid JSON on the initial messages fetch and the poll loop; stderr `[hub-talk] hub unreachable, retrying`; backoff capped at 10s. Exit **0** only after printing a peer turn. Optional `--wait-timeout <seconds>` → exit **2**. `--say` / `--inbox` / join still fail-fast on 4xx. |
| `.agents/HOH/E3.md` | This file |

Not touched: join/lobby, since-file, Convex collapse, JSON-RPC, daemons, the caller `while true` loop.

## Commands

```
npm test
node scripts/hub-talk.mjs --as cursor-peer … --wait   # PID 24612
# stop hub ≥5s, restart hub
node scripts/hub-talk.mjs --as cursor-grok … --say "D3-probe-wait-retry-ok"
node scripts/hub-talk.mjs --as cursor-peer … --wait --wait-timeout 5
```

`npm test`: **41/41 passed**. Exit 0.

## Acceptance

1. **Same PID through hub bounce.** Wait PID **24612** stayed `running` while hub :4000 was killed for ≥6s and restarted. Stderr: `[hub-talk] hub unreachable, retrying` (three times). No `--- <peer> ---` until a real message.
2. **Peer probe.** `--as cursor-grok --say "D3-probe-wait-retry-ok"` → that wait **exit_code 0** and stdout:

```
--- cursor-grok ---
D3-probe-wait-retry-ok
```

3. **Timeout.** `--wait --wait-timeout 5` with no new peer turn printed `[hub-talk] wait timeout` and **exit=2** (not 0).

GitNexus unavailable. Blast radius: LOW, this script only.
